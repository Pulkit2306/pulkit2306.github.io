# pulkit2306.github.io

Personal landing page, served via [GitHub Pages](https://pages.github.com/).

Currently a "work in progress" placeholder with a Matrix-style terminal theme, built as a single static `index.html` (no build step, no dependencies).

## Deploy

Push to the `main` branch of this repo and enable GitHub Pages (Settings → Pages → Deploy from branch → `main`).
