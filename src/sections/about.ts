export function renderAbout(): string {
  return `
    <section id="about">
      <div class="panel">
        <h2>&gt; about</h2>
        <p class="body-text">
          Placeholder bio — replace with a couple of sentences about your background,
          what you work on, and what you're looking for. Keep it short; this is a
          landing page, not a resume.
        </p>
      </div>
    </section>
  `;
}
